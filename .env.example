# Copy this file to .env and fill in a real secret.
# Must match `forwarding.secret` in your Velocity proxy's forwarding.secret file
# when running in modern forwarding mode.
FORWARDING_SECRET=replace-me-with-a-long-random-string
